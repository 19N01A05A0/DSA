class Solution {
    public boolean rotateString(String s, String goal) {
        String concat=s+s;
        if(s.length()==goal.length()&&concat.contains(goal))
        {
            return true;
        }
        else
        {
            return false;
        }
    }
}
