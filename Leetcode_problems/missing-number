class Solution {
    public int missingNumber(int[] nums) {
        int len=nums.length;
        int sm=0;
        for(int i=0;i<len;i++)
        {
            sm+=nums[i];
        }
        int totsm=(len*(len+1))/2;
        return totsm-sm;
    }
}
